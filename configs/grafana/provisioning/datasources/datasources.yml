apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: chronoshield-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: GET
      timeInterval: "15s"
    editable: true

  - name: Loki
    type: loki
    uid: chronoshield-loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    jsonData:
      maxLines: 1000
    editable: true
